# @signal/widget-core

Preact + Shadow DOM embed. Build:

```bash
pnpm --filter @signal/widget-core build
pnpm --filter @signal/widget-core size
```

IIFE lands in `dist/embed.global.js` and is copied to `apps/web/public/embed.js`.
